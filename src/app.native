<column gap="16" padding="20">
  <if test="{not configured}">
    <column gap="16" grow="1" main="center">
      <column gap="6">
        <text size="display">Welcome to Sarvam Translate</text>
        <text foreground="text_muted" wrap="true">Translate naturally across Indian languages with Mayura.</text>
      </column>

      <if test="{checkingKey}">
        <card background="surface" radius="lg">
          <row gap="8" padding="14" cross="center">
            <spinner />
            <text>Checking secure Keychain storage...</text>
          </row>
        </card>
      </if>
      <else>
      <card background="surface" radius="lg">
        <column gap="14" padding="16">
          <column gap="4">
            <text size="heading">Connect Sarvam AI</text>
            <text foreground="text_muted" wrap="true">Your API key stays in your macOS Keychain.</text>
          </column>
          <column gap="6">
            <text>Sarvam API key</text>
            <text-field text="{apiKeyText}" placeholder="Paste your api-subscription-key" on-input="api_key_edit" autofocus="true" />
          </column>
          <if test="{failed}">
            <text foreground="destructive" wrap="true">Enter a valid API key. If it is valid, check Keychain access and try again.</text>
          </if>
          <if test="{savingKey}">
            <button variant="primary" disabled="true">Saving API key...</button>
          </if>
          <else>
            <button variant="primary" on-press="configure_api_key">Save API key</button>
          </else>
        </column>
      </card>
      </else>
    </column>
  </if>
  <else>
    <row cross="center">
      <column grow="1" gap="4">
        <text size="heading">Sarvam Translate</text>
        <text foreground="text_muted">Real Indian language, in your voice</text>
      </column>
      <if test="{savingKey}">
        <text foreground="text_muted">Saving API key...</text>
      </if>
      <else>
        <if test="{keySaveFailed}">
          <text foreground="destructive">API key not saved</text>
        </if>
      </else>
      <button size="sm" variant="ghost" on-press="change_api_key">Change key</button>
    </row>

    <card background="surface" radius="lg">
      <column gap="10" padding="14">
        <row gap="12" cross="center">
          <text foreground="text_muted" grow="1">Translate to</text>
          <toggle-group gap="2" label="Translation language">
            <toggle-button size="sm" selected="{targetLabel == 'Hindi'}" on-toggle="set_hindi">Hindi</toggle-button>
            <toggle-button size="sm" selected="{targetLabel == 'Tamil'}" on-toggle="set_tamil">Tamil</toggle-button>
            <toggle-button size="sm" selected="{targetLabel == 'Telugu'}" on-toggle="set_telugu">Telugu</toggle-button>
            <toggle-button size="sm" selected="{targetLabel == 'English'}" on-toggle="set_english">English</toggle-button>
          </toggle-group>
        </row>
        <separator />

        <row gap="12" cross="center">
          <text foreground="text_muted" grow="1">Tone</text>
          <toggle-group gap="2" label="Translation tone">
            <toggle-button size="sm" selected="{tone == 'formal'}" on-toggle="set_formal">Formal</toggle-button>
            <toggle-button size="sm" selected="{tone == 'modern'}" on-toggle="set_modern">Modern</toggle-button>
            <toggle-button size="sm" selected="{tone == 'classic'}" on-toggle="set_classic">Classic</toggle-button>
            <toggle-button size="sm" selected="{tone == 'code_mixed'}" on-toggle="set_code_mixed">Code mixed</toggle-button>
          </toggle-group>
        </row>
        <separator />

        <row gap="12" cross="center">
          <text foreground="text_muted" grow="1">Numerals</text>
          <toggle-group gap="2" label="Numeral format">
            <toggle-button size="sm" selected="{nativeNumerals}" on-toggle="set_native_numerals">Native</toggle-button>
            <toggle-button size="sm" selected="{not nativeNumerals}" on-toggle="set_international_numerals">International</toggle-button>
          </toggle-group>
        </row>
      </column>
    </card>

    <column gap="6">
      <text foreground="text_muted">Text to translate</text>
      <textarea text="{inputText}" placeholder="Type it how you would say it..." on-input="input_edit" height="128" />
    </column>

    <if test="{loading}">
      <button variant="primary" disabled="true">Translating to {targetLabel}...</button>
    </if>
    <else>
      <button variant="primary" on-press="translate">Translate to {targetLabel}</button>
    </else>

    <card background="surface" radius="lg">
      <column gap="8" padding="14">
        <text>Translation</text>
        <text foreground="text_muted">{status}</text>
        <text wrap="true">{output}</text>
      </column>
    </card>
  </else>
</column>
